{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "gsmarc://saf/contracts/report/v1",
  "title": "Harness report envelope contract",
  "description": "Generic report envelope shared by every harness function. Function output contracts extend this envelope with function-specific result properties; error outcomes use the same envelope and optional error detail. Every function's `out` value is a concrete report: the exact object returned by the service, rendered by the command, forwarded unchanged by the hook adapter, and persisted byte-identically as the log entry's `report` (see log-entry.schema.json).",
  "type": "object",
  "required": [
    "context",
    "outcome"
  ],
  "properties": {
    "context": {
      "$ref": "gsmarc://saf/contracts/context/v1",
      "description": "Replay and correlation context identifying the function, session, and workflow instance."
    },
    "outcome": {
      "type": "object",
      "required": [
        "status"
      ],
      "properties": {
        "status": {
          "type": "string",
          "minLength": 1,
          "description": "The outcome status of the function invocation."
        },
        "error": {
          "type": "object",
          "required": [
            "code",
            "message"
          ],
          "properties": {
            "code": {
              "type": "string",
              "minLength": 1,
              "pattern": "^[a-z0-9-]+$",
              "description": "Stable slug-form error code for programmatic handling. The core specification's named codes (invalid-inquiry, session-ended, session-unregistered, session-conflict, not-facilitator, unknown-workflow, step-in-flight, step-correlation-missing, condition-evaluation-failed, artifact-schema-unresolved) are normative."
            },
            "message": {
              "type": "string",
              "minLength": 1,
              "description": "Human-readable error message."
            },
            "retryable": {
              "type": "boolean",
              "description": "Whether the caller may retry the same invocation."
            }
          },
          "unevaluatedProperties": false,
          "description": "Error detail object; required when status is an error status, forbidden otherwise."
        }
      },
      "if": {
        "properties": {
          "status": {
            "$ref": "#/$defs/errorStatus"
          }
        },
        "required": [
          "status"
        ]
      },
      "then": {
        "required": [
          "error"
        ]
      },
      "else": {
        "not": {
          "required": [
            "error"
          ]
        }
      },
      "description": "The function outcome: a status plus, on error statuses, an error detail object."
    }
  },
  "$defs": {
    "errorStatus": {
      "type": "string",
      "enum": [
        "inquiry-error",
        "state-error",
        "configuration-error",
        "system-error"
      ],
      "description": "The set of error statuses every harness function may return. inquiry-error covers everything wrong with the inquiry itself — contract/domain validation failures and illegitimate calls alike, distinguished by error.code."
    }
  }
}